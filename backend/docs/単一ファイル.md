# 単一ファイル

`8 << 20` は`8 x 2^20`で8MBです。 multipart処理時にメモリに保持する上限です。
`dst` は（destination: 保存先）という意味の変数によく使われる。
`filepath.Join("./files/" , filepath.Base(file.Filename))`は `./files/`と取得したパスの最後の部分を**安全に結合**するためのメソッド